import React, { FC, ReactNode } from 'react';

interface IProps {
    children?: ReactNode
}

const Photos: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export { Photos };
