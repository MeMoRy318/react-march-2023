import React, { FC, ReactNode } from 'react';

interface IProps {
    children?: ReactNode
}

const Photo: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export { Photo };
