import React, { FC, ReactNode } from 'react';

interface IProps {
    children?: ReactNode
}

const Posts: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export { Posts };
