import React, { FC, ReactNode } from 'react';

interface IProps {
    children?: ReactNode
}

const Comment: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export { Comment };
