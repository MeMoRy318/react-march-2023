import React, { FC, ReactNode } from 'react';

interface IProps {
    children?: ReactNode
}

const PostsPage: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export { PostsPage };
