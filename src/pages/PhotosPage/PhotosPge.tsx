import React, { FC, ReactNode } from 'react';

interface IProps {
    children?: ReactNode
}

const PhotosPge: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export { PhotosPge };
